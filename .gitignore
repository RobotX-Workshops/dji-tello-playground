/venv/
.env

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

**.pt

.DS_Store
Icon
artifacts

# agent-kombat debate run dir (no trailing slash: the driver runs
# `git check-ignore -q tmp` before the dir exists, and `tmp/` would not match)
tmp
